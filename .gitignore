# OS
.DS_Store
Thumbs.db
desktop.ini

# Editors
*.swp
*~

# Logs
*.log

# Node
node_modules/

# Local-only Playwright browser-testing rig (see .claude/skills/browser-test/).
# The app itself has NO dependencies and NO build step - that invariant is
# deliberate, so the rig lives here and is never committed or deployed.
.browser-tests/
